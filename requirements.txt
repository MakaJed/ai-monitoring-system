Flask==3.0.3
numpy==1.26.4
# Prefer headless OpenCV on Linux (Pi). Keep full OpenCV elsewhere for dev.
opencv-python-headless==4.10.0.84; platform_system == "Linux"
opencv-python==4.10.0.84; platform_system != "Linux"
psutil==5.9.8
Pillow==10.4.0

# Use tflite-runtime for Raspberry Pi (ARM64/ARMv7)
tflite-runtime; platform_machine == "aarch64" or platform_machine == "armv7l"
# Install TensorFlow only on Linux non-ARM (avoid Windows/mac issues)
tensorflow==2.16.1; platform_system == "Linux" and platform_machine != "aarch64" and platform_machine != "armv7l"

adafruit-circuitpython-mlx90640==1.2.10  # Pinned: newer versions (1.3.x) have stricter timing that may prevent 8Hz
adafruit-circuitpython-bme680==3.7.13
adafruit-blinka==8.38.0
matplotlib==3.9.0
